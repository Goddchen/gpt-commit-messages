name: gpt_commit_messages
description: Let chat gpt suggest commit messages.
version: 1.0.0
repository: https://github.com/Goddchen/gpt-commit-messages

environment:
  sdk: ">=2.19.2 <3.0.0"

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
dependencies:
  args: ^2.4.0
  fpdart: ^0.5.0
  http: ^0.13.5
  logger: ^1.3.0
